import type { Metadata } from "next"; import { AppShell } from "@/src/components/layout/app-shell"; import { SiteIntelligence } from "@/src/features/sites/site-intelligence"; import { catalogService } from "@/src/services/catalog.service";
export async function generateMetadata({params}:{params:Promise<{id:string}>}):Promise<Metadata>{const{id}=await params;const site=catalogService.getSites().find(x=>x.id===id);if(!site)return{title:"Site not found | EV Atlas Thailand",openGraph:{images:[]},twitter:{images:[]}};const title=site.name+" | EV Atlas Thailand";const description="Site intelligence for "+site.district+", "+site.province+". Demo data; not real-time.";return{title,description,openGraph:{title,description,images:[]},twitter:{title,description,images:[]}}}
export default function SitePage(){return <AppShell><SiteIntelligence/></AppShell>}
