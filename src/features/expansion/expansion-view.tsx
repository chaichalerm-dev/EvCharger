"use client";
import { Bar,BarChart,CartesianGrid,Legend,ResponsiveContainer,Tooltip,XAxis,YAxis } from "recharts"; import { Database,TrendingUp } from "lucide-react"; import { catalogService } from "@/src/services/catalog.service"; import { calculateSiteScore } from "@/src/services/scoring-engine";
const MOCK_SITES=catalogService.getSites();
export function ExpansionView(){const stages=[{n:"Total",v:24},{n:"High potential",v:13},{n:"Potential",v:7},{n:"Under review",v:6},{n:"Approved",v:4},{n:"Construction",v:2},{n:"Operational",v:9}];const models=[{name:"Company-owned",Bangkok:5,Regional:3},{name:"Franchise",Bangkok:3,Regional:5},{name:"Partner",Bangkok:7,Regional:6},{name:"Branch expansion",Bangkok:2,Regional:4}];const ranked=[...MOCK_SITES].sort((a,b)=>calculateSiteScore(b.factors).overall-calculateSiteScore(a.factors).overall);return <main className="page"><div className="page-head"><div><div className="eyebrow">Growth portfolio</div><h1>Expansion Dashboard</h1><p className="page-subtitle">Portfolio readiness by stage, ownership model and location priority</p></div></div><div className="demo-banner"><Database/>Demo Data · Portfolio figures are illustrative and not a commitment or forecast.</div><div className="funnel-grid">{stages.map((x,i)=><article className="card funnel-card" key={x.n}><strong style={{color:i===1?"var(--brand)":undefined}}>{x.v}</strong><span>{x.n}</span></article>)}</div><div className="dashboard-grid"><section className="card comparison-chart"><div className="card-head"><div><h2>Pipeline by business model</h2><p>Bangkok vs regional opportunity coverage</p></div></div><ResponsiveContainer width="100%" height="82%"><BarChart data={models}><CartesianGrid stroke="var(--line)" vertical={false}/><XAxis dataKey="name" tick={{fontSize:8,fill:"var(--muted)"}}/><YAxis tick={{fontSize:8}}/><Tooltip contentStyle={{background:"var(--panel)",border:"1px solid var(--line)",fontSize:9}}/><Legend wrapperStyle={{fontSize:9}}/><Bar dataKey="Bangkok" fill="#087a5b" radius={[4,4,0,0]}/><Bar dataKey="Regional" fill="#d89416" radius={[4,4,0,0]}/></BarChart></ResponsiveContainer></section><section className="card"><div className="card-head"><div><h2>Investment priority</h2><p>Highest weighted demo opportunities</p></div><TrendingUp color="var(--brand)" size={17}/></div><div className="card-body rank-list">{ranked.slice(0,6).map((s,i)=><div className="rank-item" key={s.id}><span className="rank-no">{i+1}</span><span><strong>{s.name}</strong><span>{s.businessModel.replaceAll("_"," ")} · {s.province}</span></span><span className="site-score-cell">{calculateSiteScore(s.factors).overall}</span></div>)}</div></section></div></main>}
