import type { StationType } from "@/src/domain/models";
const assumptions={CHARGING_POINT:{investment:850000,revenue:65000,operating:22000},EV_HUB:{investment:4200000,revenue:310000,operating:102000},FULL_EV_STATION:{investment:11500000,revenue:790000,operating:285000}} as const;
export function simulateBusiness(type:StationType,demandScore:number){if(!(type in assumptions))return null;const base=assumptions[type as keyof typeof assumptions];const factor=.65+demandScore/250;const monthlyRevenue=Math.round(base.revenue*factor);const monthlyOperatingCost=base.operating;const monthlyCash=Math.max(1,monthlyRevenue-monthlyOperatingCost);return{investment:base.investment,monthlyRevenue,monthlyOperatingCost,annualRoiPercent:+((monthlyCash*12/base.investment)*100).toFixed(1),paybackYears:+(base.investment/(monthlyCash*12)).toFixed(1)}}
