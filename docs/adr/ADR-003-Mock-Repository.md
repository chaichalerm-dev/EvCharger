# ADR-003: Mock repositories

Status: Accepted.

Fixtures are accessed through repository/service contracts. Only prototype submissions use localStorage through a repository. Future API adapters replace implementations, not UI features.
