"use client";
import { useTheme } from "next-themes"; import { Database,Globe2,Moon,ShieldCheck } from "lucide-react"; import { useApp } from "@/src/store/app-context"; import type { DemoRole } from "@/src/config/permissions";
export function SettingsView(){const{theme,setTheme}=useTheme();const{language,setLanguage,role,setRole}=useApp();return <main className="page"><div className="page-head"><div><div className="eyebrow">Experience controls</div><h1>Settings</h1><p className="page-subtitle">Language, theme and prototype access preferences</p></div></div><div className="settings-grid"><section className="card setting-card"><div className="card-head" style={{padding:0}}><div><h2>Appearance</h2><p>Stored as a device-local preference</p></div><Moon size={17}/></div><div className="setting-row"><div><strong>Color theme</strong><span>Light, dark or follow system</span></div><select className="select" value={theme} onChange={e=>setTheme(e.target.value)}><option value="light">Light</option><option value="dark">Dark</option><option value="system">System</option></select></div><div className="setting-row"><div><strong>Language</strong><span>Interface language architecture</span></div><select className="select" value={language} onChange={e=>setLanguage(e.target.value as "en"|"th")}><option value="en">English</option><option value="th">ไทย</option></select></div></section><section className="card setting-card"><div className="card-head" style={{padding:0}}><div><h2>Prototype authorization</h2><p>UX-only permission demonstration</p></div><ShieldCheck size={17}/></div><div className="setting-row"><div><strong>Demo role</strong><span>Not secure authentication</span></div><select className="select" value={role} onChange={e=>setRole(e.target.value as DemoRole)}>{["ADMIN","BUSINESS_DEVELOPMENT","ANALYST","PARTNER","VIEWER"].map(x=><option key={x}>{x}</option>)}</select></div><div className="callout warning" style={{marginTop:13}}>Frontend role checks only control prototype UX. A future backend must independently authenticate users and enforce every authorization decision.</div></section><section className="card setting-card"><div className="card-head" style={{padding:0}}><div><h2>Localization readiness</h2><p>Thailand first, extensible to ASEAN</p></div><Globe2 size={17}/></div><div className="setting-row"><div><strong>Primary market</strong><span>Driven by geographic coverage config</span></div><span className="badge green">Thailand</span></div><div className="setting-row"><div><strong>Typography</strong><span>Noto Sans Thai + Noto Sans</span></div><span className="badge">Bundled</span></div></section><section className="card setting-card"><div className="card-head" style={{padding:0}}><div><h2>Data mode</h2><p>No backend or database required</p></div><Database size={17}/></div><div className="setting-row"><div><strong>Repository implementation</strong><span>Replaceable through interfaces</span></div><span className="badge amber">Mock</span></div><div className="setting-row"><div><strong>Submission persistence</strong><span>Device-local; non-secure</span></div><span className="badge amber">localStorage</span></div></section></div></main>}
