"use client";
import { useParams } from "next/navigation"; import Link from "next/link"; import { ArrowLeft,Building2,MapPin } from "lucide-react"; import { catalogService } from "@/src/services/catalog.service";
const MOCK_BRANCHES=catalogService.getBranches(); const MOCK_PARTNERS=catalogService.getPartners();
export function PartnerDetail(){const{id}=useParams<{id:string}>();const partner=MOCK_PARTNERS.find(p=>p.id===id)??MOCK_PARTNERS[0];const branches=MOCK_BRANCHES.filter(b=>b.partnerId===partner.id).sort((a,b)=>b.siteScore-a.siteScore);return <main className="page"><Link className="btn" href="/partners"><ArrowLeft/>All partners</Link><section className="detail-head" style={{marginTop:13}}><div className="detail-head-top"><div><span className="badge green">{partner.status}</span><h1>{partner.name}</h1><p className="page-subtitle">{partner.type.replaceAll("_"," ")} · {partner.businessType}</p><div className="detail-meta"><span><Building2/>{partner.branchCount} branches</span><span>Contract: {partner.contractStatus}</span><span>Contact: {partner.contact}</span></div></div><div className="detail-score"><strong>{branches.length}</strong><span>EVALUATED DEMO BRANCHES</span></div></div></section><section className="card"><div className="card-head"><div><h2>Branch ranking</h2><p>Each branch is scored independently</p></div><Link className="btn" href="/branches">All branches</Link></div>{branches.length?<div className="table-wrap"><table className="data-table"><thead><tr><th>Rank</th><th>Branch</th><th>Available area</th><th>Parking</th><th>Electrical</th><th>Score</th><th>Recommendation</th><th>Status</th></tr></thead><tbody>{branches.map((b,i)=><tr key={b.id}><td>{i+1}</td><td><strong className="table-title">{b.name}</strong><span className="table-sub"><MapPin size={10}/>{b.district}, {b.province}</span></td><td>{b.availableAreaSqm??"Unknown"} m²<span className="table-sub">Estimated</span></td><td>{b.parkingSpaces??"Unknown"}</td><td>{b.electricalStatus}</td><td><span className="site-score-cell">{b.siteScore}</span></td><td>{b.recommendedStation.replaceAll("_"," ")}</td><td><span className={`badge ${b.installationStatus.toLowerCase()}`}>{b.installationStatus.replaceAll("_"," ")}</span></td></tr>)}</tbody></table></div>:<div className="status-card"><Building2/><strong>No evaluated branches</strong><p>Partner branches will appear after submission and validation.</p></div>}</section></main>}
