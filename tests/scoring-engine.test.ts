import { describe,expect,it } from "vitest"; import { calculateSiteScore } from "@/src/services/scoring-engine";
describe("scoring engine",()=>{it("calculates a deterministic weighted score",()=>{const score=calculateSiteScore({demand:90,competition:70,accessibility:80,poi:60,infrastructure:50,floodRisk:40,siteArea:30,businessPotential:20});expect(score.overall).toBe(62);expect(score.configurationVersion).toBe("demo-v1.0")});it("keeps the score within a normalized range",()=>{expect(calculateSiteScore({demand:100,competition:100,accessibility:100,poi:100,infrastructure:100,floodRisk:100,siteArea:100,businessPotential:100}).overall).toBe(100)})});
