# Future server/BFF configuration only. Public prototype providers are configured in Settings.
API_BASE_URL=

# Keep production provider secrets server-side. Never prefix secrets with NEXT_PUBLIC_.
TOMTOM_API_KEY=
WORLDPOP_API_KEY=
OPEN_METEO_API_KEY=

# Optional trusted deployment origin for absolute metadata.
NEXT_PUBLIC_APP_ORIGIN=
