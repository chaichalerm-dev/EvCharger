import { MOCK_SITES } from "@/src/data/mock/sites"; import { calculateSiteScore } from "./scoring-engine";
export const dashboardService={getSummary(){const scores=MOCK_SITES.map(s=>calculateSiteScore(s.factors).overall);return{totalOpportunities:MOCK_SITES.length+16,highPotential:scores.filter(x=>x>=75).length+7,underAnalysis:MOCK_SITES.filter(s=>s.opportunityStatus==="UNDER_ANALYSIS").length+4,approved:MOCK_SITES.filter(s=>s.opportunityStatus==="APPROVED").length+3,partnerLocations:38,existingBranches:17,competitorStations:126,evStations:284,averageScore:Math.round(scores.reduce((a,b)=>a+b,0)/scores.length)}}};
