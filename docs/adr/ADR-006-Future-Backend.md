# ADR-006: Future backend

Status: Deferred with defined boundary.

Introduce NestJS/Node.js or Go behind REST only when multi-user persistence, security, provider sync, or operational scale requires it. Backend choice must not leak into feature UI.
